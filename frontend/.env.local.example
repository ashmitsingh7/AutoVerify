# Base URL of the AutoVerify FastAPI backend.
# Local dev (matches backend/Dockerfile default port):
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (Railway) example:
# NEXT_PUBLIC_API_URL=https://autoverify-api.up.railway.app
